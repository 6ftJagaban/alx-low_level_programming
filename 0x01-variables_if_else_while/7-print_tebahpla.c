#include <stdio.h>
/**
 * main - ALX Coding Project
 * DESCRIPTION - Reversing the alphabets
 * Return:  Always 0 (Success)
 */
int main(void)
{
	char h;

	h = 'z';
	while
		(h >= 'a') {
			putchar(h);
			h--;
		}
	putchar('\n');
		return (0);
}
