#include <stdio.h>
/**
*main - ALX Coding Project
*Description - Tired as
*Return - Always 0
*/
int main()
{
	char tsu;
	tsu = 'a';
while
	(tsu <= 'z') {
if (( tsu != 'e' && tsu != 'q') && tsu <= 'z')
	putchar(tsu);
	tsu++;
}

	putchar('\n');
return(0);
}
