#include <stdio.h>
/**
*main - ALX Coding Project
*Description - Tired as
*Return: Always 0
*/
int main(void)
{
	char u;

	u = 'a';

while
	(u <= 'z') {
		if ((u != 'e' && u != 'q') && u <= 'z')
			putchar(u);
		u++;
}

	putchar('\n');
	return (0);
}
